IF (AVANGO_DISTRIBUTION_SUPPORT)

    FILE (GLOB _AVANGO_UNITTEST_NETWORK_SRC RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}
        *.cpp
        *.h
    )

    SET (_EXECUTABLE_FILENAME unittest_network)

    ADD_EXECUTABLE (${_EXECUTABLE_FILENAME} 
	    ${_AVANGO_UNITTEST_NETWORK_SRC}
    )

    TARGET_LINK_LIBRARIES (${_EXECUTABLE_FILENAME} avango_core)

    INCLUDE_DIRECTORIES( ${INCLUDE_PATHS} 
      ${CMAKE_SOURCE_DIR}/avango-unittest/include 
    )

ENDIF (AVANGO_DISTRIBUTION_SUPPORT)