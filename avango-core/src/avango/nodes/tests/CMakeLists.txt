FILE(GLOB _AVANGO_UNITTEST_NODES_SRC RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}
    *.cpp
    *.h
  )

SET(_EXECUTABLE_FILENAME unittest_nodes)

ADD_EXECUTABLE(${_EXECUTABLE_FILENAME}
	${_AVANGO_UNITTEST_NODES_SRC}
)

TARGET_LINK_LIBRARIES (${_EXECUTABLE_FILENAME} avango_unittest)